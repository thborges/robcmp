int16 containsstr(char[] str, char[] substr) {
    i = uint16 (0);
    
    while i < str.size {
        j = uint16 (0);

        while (str[i + j] == substr[j]) {
            j++;
        }

        if j == substr.size {
            return i;
        }
        i++;
    }
    return -1;
}