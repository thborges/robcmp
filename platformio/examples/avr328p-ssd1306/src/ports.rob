
enum port_mode { input, output }

interface digitalport {
	void mode(port_mode m);
	void set(bool v);
	bool get();
}

interface analogport {
	void mode(port_mode m);
	void set(int16 v);
	int16 get();
}
