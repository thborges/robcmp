/*
 * Robcmp examples: Blink
 * Blinks the board default led
 */

use intfs.ports;

mmcu = mcu();
led = digitalport();

int16 main() {

	led.mode(port_mode.output);

	loop {
		led.set(true);
		mmcu.wait_ms(500);
		led.set(false);
		mmcu.wait_ms(500);
	}
}
