
int8 main() {
    block = {false:2};
    i = 2;
    while (i > 0) {
        row = i;
        block[row] = true;
        i--;
    }

    row = 0;
    while row < block.size {
        block[row] = false;
        row++;
    }

    return block[0];
}

